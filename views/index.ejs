<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>User database looks like:</h1>
    
    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>username</th>
                <th>email</th>
            </tr>
        </thead>
            <% for(let i=0;i<data.length;i++){ %>
                <tr>
                    <td><%=data[i].id%></td>
                    <td><%=data[i].username%></td>
                    <td><%=data[i].email%></td>
                    <td><a href="user/<%=data[i].id%>/update">update</a></td>
                    <td>
                        <form method="POST" action="/user/<%=data[i].id%>?_method=DELETE">
                            <button class="del">Delete post</button>
                        </form>
                    </td>
                </tr> 
            <% } %>
        
    </table>
</body>
</html>

